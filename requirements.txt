# KAG 项目实际依赖 - 基于 venv 环境分析
# 只包含项目中实际使用的包

# 标准库（无需安装）
# - json, re, os, sys, argparse, logging, pathlib, datetime
# - abc, typing, enum, subprocess, platform, tempfile, base64
# - itertools, pickle

# 文档处理
PyMuPDF>=1.26.0  # fitz 模块，用于 PDF 处理

# LLM 框架
langchain>=0.3.0
langchain-community>=0.3.0
langchain-core>=0.3.0
langchain-openai>=0.3.0
langchain-experimental>=0.3.0  # SQLDatabaseChain
langgraph>=0.2.0  # LangGraph 框架
qwen_agent>=0.0.29  # Qwen Agent 框架
langchain-huggingface>=0.1.0  # HuggingFace 集成
langchain-chroma>=0.1.0  # Chroma 向量数据库集成
langchain-text-splitters>=0.3.0  # 文本分割器

# OpenAI
openai>=1.82.0

# 数据库
neo4j>=5.28.0
chromadb>=1.0.0

# 机器学习
sentence-transformers>=4.1.0
transformers>=4.51.0  # generate_metadata.py 中使用
torch>=2.5.0  # PyTorch 支持

# 数据处理
pandas>=2.0.0
numpy>=1.26.0,<2.0.0
scikit-learn>=1.7.0  # 机器学习工具

# 可视化（仅在 render_graph.py 中使用）
networkx>=3.0,<3.5
pyvis>=0.3.0
gradio>=5.23.0

# 配置和工具
pydantic>=2.9.0
PyYAML>=6.0
python-dotenv>=1.1.0
tqdm>=4.67.0

# 异步和网络
aiohttp>=3.12.0  # 异步 HTTP 客户端
httpx>=0.28.0  # HTTP 客户端
fastapi>=0.115.0  # Web API 框架

# 工具和实用程序
python-dateutil>=2.9.0  # 日期时间处理
requests>=2.32.0  # HTTP 请求
rich>=14.0.0  # 终端美化

# 可选依赖（根据实际需要安装）
# - accelerate>=1.7.0  # 如果需要模型加速
# - beautifulsoup4>=4.13.0  # 如果需要 HTML 解析
# - jieba>=0.42.0  # 如果需要中文分词
# - nltk>=3.9.0  # 如果需要 NLP 工具
# - spacy>=3.8.0  # 如果需要高级 NLP
# - uvicorn>=0.34.0  # 如果需要 ASGI 服务器
# - loguru>=0.7.0  # 如果需要日志 